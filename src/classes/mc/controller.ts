import Model from './model';
import Constants from '../constants';

export default class Controller
{ 

  // private G: Constants;
  // private model: Model;
  // public emitter:Phaser.Events.EventEmitter;

  // constructor()
  // {
  //   this.G = new Constants();
  //   this.emitter.emit(this.G.setScore,this.setScore);
  //   this.emitter.emit(this.G.upPoints,this.upPoints);
  //   this.emitter.emit(this.G.toggleSound,this.toggleSound);
  //   this.emitter.emit(this.G.toggleMusic,this.toggleMusic);
  // }

  // toggleSound(val){
  //   this.model.soundOn=val;
  // }

  // toggleMusic(val){
  //   this.model.musicOn=val;
  // }

  // setScore(score)
  // {
  //   this.model.score=score;
  // }

  // upPoints(points)
  // {
  //   let score=this.model.score;
  //   score+=points;
  //   this.model.score=score;
  // }
}